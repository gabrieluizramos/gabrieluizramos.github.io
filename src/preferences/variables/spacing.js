const SPACING = 15;

export default {
  half: `${SPACING / 2}px`,
  default: `${SPACING}px`,
  onehalf: `${SPACING + (SPACING / 2)}px`,
  double: `${SPACING * 2}px`,
  triple: `${SPACING * 3}px`,
  quadruple: `${SPACING * 4}px`
}
